# The engine (engine/*.py), the site builders (scripts/*.py) and tests.
skyfield==1.54
numpy==2.4.3
contourpy==1.3.3
pytest==9.1.1
Pillow==12.3.0   # build time only: the link-preview images (scripts/build_og.py)
